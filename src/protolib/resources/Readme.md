# ASSISTANT INSTRUCTIONS — Follow after cloning

You are reading this because a new project was just cloned from proto-lib.
The project is: **Protolib** — alias: `proto`

The package is currently a generic boilerplate copy of protolib.
Do NOT read the source files for project context — they contain no target-specific information yet.

---

## Your task: write a real Readme for this project

### Step 1 — Verify the setup works
Run:
```shell
proto info -i python package
```
This shows the current Python and package state of the cloned project.

### Step 2 — Infer and ask
#### Blueprint
Check for a blueprint document in the project directory (e.g. `BLUEPRINT.md`, `BLUEPRINT_<target_project>.md`, etc.). If it exists, read it to understand the project goals and features.

#### Inference and questions
Use the project name (`Protolib`) and alias (`proto`) to make an initial guess about what this project is for
Then ask the user the famous 5 W's to clarify the project purpose and features.

### Step 3 — Replace this file
Overwrite new Readme.md for the target project containing:
- Project name and description
- Installation steps (`uv sync`, `activate`)
- Basic usage with the correct alias (`proto`)
- **Next Steps** chapter based on the user's answers

---
*This file is generated by the protolib clone process. Replace it with a project-specific Readme.*
